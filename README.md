# Basic-RAG
A basic RAG-based research paper assistant built with LangChain, FAISS, Hugging Face embeddings, and Groq. It loads PDF documents, splits them into chunks, performs semantic similarity search, and uses an LLM to generate accurate answers based only on the retrieved context.
